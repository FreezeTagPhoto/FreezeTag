# Usage
* uses the Gemini API to generate tags. 
* Requires an API key to be present in a .env file
* It will use the first .env file it finds while iterating through the parent directories

## Api Key Format 
GEMINI-API-KEY=YOUR-API-KEY